/*
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/*global getParticipantRegistry getAssetRegistry getFactory */

/**
 * create a Milk Bank Factory asset
 * @param {org.donormilk.openMilkBank} tx
 * @transaction
 */
async function openMilkBank(tx) { // eslint-disable-line no-unused-vars
}

/**
 * Create a Hospital Factory asset
 * @param {org.donormilk.openHospital} tx
 * @transaction
 */
async function openHospital(tx) { // eslint-disable-line no-unused-vars
}

/**
 * Create a Laboratory Factory asset
 * @param {org.donormilk.openLaboratory} tx
 * @transaction
 */
async function openLaboratory(tx) { // eslint-disable-line no-unused-vars
}

/**
 * Update a Milk Bank Factory asset
 * @param {org.donormilk.updateMilkBank} tx
 * @transaction
 */
async function updateMilkBank(tx) { // eslint-disable-line no-unused-vars
}

/**
 * Update a Hospital Factory asset
 * @param {org.donormilk.updateHospital} tx
 * @transaction
 */
async function updateHospital(tx) { // eslint-disable-line no-unused-vars
}

/**
 * Update a Laboratory Factory asset
 * @param {org.donormilk.updateLaboratory} tx
 * @transaction
 */
async function updateLaboratory(tx) { // eslint-disable-line no-unused-vars
}

/**
 * Create a certification asset
 * @param {org.donormilk.createCertification} tx
 * @transaction
 */
async function createCertification(tx) { // eslint-disable-line no-unused-vars
}

/**
 * Link a Facility asset to a certification asset
 * @param {org.donormilk.grantCertification} tx
 * @transaction
 */
async function grantCertification(tx) { // eslint-disable-line no-unused-vars
}

/**
 * Unlink a Facility asset to a certification asset
 * @param {org.donormilk.revokeCertification} tx
 * @transaction
 */
async function revokeCertification(tx) { // eslint-disable-line no-unused-vars
}

/**
 * Unlink a Facility asset to a certification asset
 * @param {org.donormilk.revokeCertification} tx
 * @transaction
 */
async function revokeCertification(tx) { // eslint-disable-line no-unused-vars
}

/**
 * Create a new Milk asset
 * @param {org.donormilk.newRawAsset} tx
 * @transaction
 */
async function newRawAsset(tx) { // eslint-disable-line no-unused-vars
}

/**
 * Locks an asset as in transfer
 * @param {org.donormilk.assetTransfer} tx
 * @transaction
 */
async function assetTransfer(tx) { // eslint-disable-line no-unused-vars
}

/**
 * Moves an asset from in transfer to received
 * @param {org.donormilk.assetReceived} tx
 * @transaction
 */
async function assetReceived(tx) { // eslint-disable-line no-unused-vars
}

/**
 * Create a new Milk asset from a list of existing Milk assets
 * @param {org.donormilk.milkPooling} tx
 * @transaction
 */
async function milkPooling(tx) { // eslint-disable-line no-unused-vars
}

/**
 * Cascade and escalate a specific defect to participants involved
 * @param {org.donormilk.signalDefect} tx
 * @transaction
 */
async function signalDefect(tx) { // eslint-disable-line no-unused-vars
}

/* Demo Specific transaction */

/**
 * Setup base participants and facilities for the demo network
 * @param {org.donormilk.setupDemo} tx
 * @transaction
 */
async function setupDemo(tx) { // eslint-disable-line no-unused-vars
}